// deps
import styled from 'styled-components';

export const UnemployedContainer = styled.div`
  display: flex;
  flex-direction: column;

  color: white;
  text-align: center;
  max-width: 15rem;

  b {
    font-size: 1rem;
  }

  p {
    font-size: 0.75rem;
  }
`;
